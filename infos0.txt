pip install pythonnet
pip install pyinstaller
pip install -r requirements.txt

pip freeze > requirements.txt


- **Build :**
    ```sh
    pyinstaller --onefile --windowed --noupx --version-file=file_version.txt --icon=app.ico --name "Auto Button Clicker" main.py
    ```
