

pip install pythonnet
pip install pyinstaller
pip install -r requirements.txt

pip freeze > requirements.txt

Before to get exe run , if setup exists: 
   ```sh
    pip install -e .
   ```
- **Basic build:**
    ```sh
    pyinstaller --windowed main.py 
    ```
- **Build with a custom icon:**
    ```sh
    pyinstaller --onefile --windowed --noupx --version-file=file_version.txt --icon=app.ico --name "Auto Button Clicker" main.py
    ```

    pyinstaller --onefile --windowed --add-data "assets;assets" --icon=app.ico --name "MyApp" adds-route.py
         pip install pyinstaller